import { Color2Directive } from './color2.directive';

describe('Color2Directive', () => {
  it('should create an instance', () => {
    const directive = new Color2Directive();
    expect(directive).toBeTruthy();
  });
});
